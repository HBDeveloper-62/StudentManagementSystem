@{
    ViewData["Title"] = "Upload Syllabus";
    Layout = "~/Views/Shared/_ClerkLayout.cshtml";
}

<div class="container py-4">
    <h3 class="mb-3">@ViewData["Title"]</h3>

    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Message"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <form asp-action="UploadSyllabusPost" method="post" enctype="multipart/form-data">
        <div class="mb-3">
            <label for="syllabusFile" class="form-label">Select syllabus file</label>
            <input type="file" class="form-control" id="syllabusFile" name="uploadedFile" required />
        </div>

        <button type="submit" class="btn btn-primary">Upload</button>
        <a asp-controller="Clerk" asp-action="Dashboard" class="btn btn-secondary">Back to Dashboard</a>

    </form>
</div>
